#ifndef __LAPLACIAN_HPP__
#define __LAPLACIAN_HPP__

int Laplacian(const unsigned char*, unsigned char*, int , int, int);
int Laplacian_(const unsigned char*, unsigned char*, int , int, int);
double variance(unsigned char* src, int width, int height);
// void display(unsigned char*, int width, int height);

#endif
